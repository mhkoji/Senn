#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.kkc-engine.3847071133
  (:use :cl))
(in-package :ros.script.kkc-engine.3847071133)
(ql:quickload :senn-bin-kkc-engine)

(defun main (&rest argv)
  (declare (ignorable argv))
  (senn.bin.kkc-engine:main))
;;; vim: set ft=lisp lisp:
