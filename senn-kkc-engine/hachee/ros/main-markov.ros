#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.engine.3847071133
  (:use :cl))
(in-package :ros.script.engine.3847071133)
(ql:quickload :senn-kkc-engine-hachee-markov)

(senn-kkc-engine.hachee.engine.markov:set-kkc)

;; ros dynamic-space-size=2048 -- dump executable main-markov.ros -o kkc-engine
(defun main (&rest argv)
  (declare (ignorable argv))
  (senn-kkc-engine.hachee.engine.markov:main))
;;; vim: set ft=lisp lisp:
